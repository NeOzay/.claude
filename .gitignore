# Secrets
.credentials.json

# Fichiers générés / backups
settings.json.bak*
# La sauvegarde du dernier `list-dir reseed` : une marche arrière LOCALE et
# immédiate. L'historique plus ancien est le travail de git, pas le sien.
**/.list/backup/
hooks/rtk-rewrite.sh

# Données d'usage / caches locaux
history.jsonl
stats-cache.json
mcp-needs-auth-cache.json
cache/
backups/
file-history/
paste-cache/
shell-snapshots/

# État runtime du daemon
daemon/
daemon.log
daemon.lock
daemon.status.json

# Sessions / exécution, spécifiques à la machine
sessions/
projects/
session-env/
ide/
jobs/
debug/
downloads/

# Télémétrie
telemetry/

# État transitoire
.caveman-active
.last-cleanup
.last-update-result.json
plugins/marketplaces/
plugins/known_marketplaces.json
tasks/

__pycache__/
.ruff_cache/
