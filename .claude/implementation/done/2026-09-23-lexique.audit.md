---
slug: lexique
---

## 2026-09-23 — clôture — `09573af`

**Verdict** : RÉSERVES

Périmètre audité : `git diff master...lexique` au commit `09573afb147670c2dc448873dcb2d39d539edc29`,
arbre propre, 24 fichiers, +1302 −2. Étapes 1 à 4 cochées `[x]` et commitées (`0dcb76c`, `183e972`,
`06c335a`, `09573af`). Brief présent et lu ; le suivi reprend ses critères à l'identique, avec un
élargissement daté (contrôle 7 de `check_pipeline.py`).

### Vérifications exécutées

- `.venv/bin/python -m pytest skills/lexique/scripts/tests -q` → 37 passés, 0 échec
- `.venv/bin/python -m pytest scripts/tests -q` → 120 passés, 0 échec (dont les 2 nouveaux tests du
  contrôle 7)
- `lexique liste; echo $?` (à la racine) → 19 lignes `global`, avis stderr « local : aucun lexique »,
  sortie 0
- `.venv/bin/python scripts/check_pipeline.py` → 9 contrôles verts, « Pipeline conforme. », sortie 0
  (contrôle 7 : 29 chemins de skill cités, tous existent)
- `python3 scripts/sante_skills.py` → sortie 0
- `grep -n '@' CLAUDE.md` → `1:@RTK.md`, `2:@skills/lexique/instructions.md`, `3:@LEXIQUE.md`
- `git diff --stat master...lexique -- skills/gabarit skills/list-dir skills/implementation-tracker`
  → vide
- `uvx ruff check .` sur `git archive master` puis sur `git archive 09573af` → 6 erreurs / 6
  erreurs, **ensembles identiques** (diff des sorties concises sans numéros de ligne : vide)
- `uvx --with pytest basedpyright` sur les deux mêmes arbres → 77 / 77 sans `.venv`, puis **25 / 25**
  avec le `.venv` du dépôt lié dans chaque arbre (la référence du suivi) ; ensembles d'erreurs
  identiques dans les deux mesures. Aucune erreur dans un fichier du chantier.
- `(cd skills/lexique && uvx --with pytest basedpyright)` → 0 erreur ; `uvx ruff check` dans
  `skills/lexique` → « All checks passed! »
- `python3 -c "import json; json.load(open('settings.json'))"` → JSON valide
- Commande exacte du hook (`sh -c 'python3 $HOME/.claude/skills/lexique/scripts/lexique-cli.py
  session'`, `CLAUDE_PROJECT_DIR` sur un projet temporaire muni d'un `.claude/LEXIQUE.md`, lancée
  depuis `/`) → imprime l'en-tête puis le tableau local, sortie 0
- Bout en bout n° 3 du plan : un lexique local portant `SUIVI` (variante de casse de `Suivi`) et
  `Essai` en double → `lexique liste --projet $d` sort en 1 et nomme les deux conflits ; `lexique
  session` les annonce sous « ALERTE » et sort en 0
- `lexique` sans argument, `lexique bidule` → usage, sortie 2
- `lexique init --projet $e` → 0 et chemin sur stdout ; relancé → 1, « rien n'est écrasé » ;
  `lexique liste` sur le fichier posé → 0, avis « lexique vide »
- Critère « une session neuve a le lexique local en contexte » → NON EXÉCUTÉE : ouvrir une session
  Claude Code depuis un contexte isolé n'est pas possible sans écrire hors du fichier d'audit ; seul
  le mécanisme (commande du hook, déclaration dans `settings.json`) est vérifié, voir R2

### Conformité à l'intention

- Critère « `grep -n '@' CLAUDE.md` → instructions importées, puis global puis local » : atteint,
  vérifié pour les instructions et le global (ordre `@RTK.md`, instructions, `@LEXIQUE.md`). Le local
  passe par le hook `SessionStart`, que le grep ne peut pas montrer : constat en deux temps, arbitré
  par l'utilisateur au journal (verdict `plan-reviewer`). L'ordre global → local repose sur
  l'observation de l'utilisateur (voir R2).
- Critère « `LEXIQUE.md` à la racine, tableau `terme | définition courte | lien`, un lien par
  ligne » : atteint, vérifié. 19 lignes, chacune avec un lien Markdown ; toutes les cibles existent
  (contrôle 7) et les six ancres correspondent à des titres réels (`#le-contrat`, `#lestampille`,
  `#les-marqueurs`, `#un-élément`, `#format-détape-et-délégabilité`, `#frontmatter`).
- **R1** — Critère « termes tirés de `gabarit`, `list-dir` et `implementation-tracker`, proposés
  puis validés un à un » : **invérifiable** sur pièces. La provenance est vérifiée : chacun des 19
  termes apparaît dans au moins une des trois skills (Brief et Hors-périmètre sont liés à
  `intent-brief` mais employés dans `implementation-tracker`, 6 et 4 fichiers). La validation un à
  un n'est attestée que par le journal (« 19 termes validés par l'utilisateur ») : aucun artefact ne
  la trace. C'est à l'utilisateur de la confirmer.
- **R2** — Critère « une session neuve a le lexique local en contexte, sans action de
  l'utilisateur » : **non exécuté par l'audit**. Le mécanisme est vérifié (hook déclaré à côté de
  `sante_skills.py`, JSON valide, commande exacte qui imprime le lexique local et sort en 0 même sur
  un lexique en conflit, malformé ou illisible). La session neuve, l'ordre instructions → global →
  local et la connaissance du terme local sont attestés par l'utilisateur au journal de l'étape 3.
  La limite des sous-agents (ils ne reçoivent rien) est consignée en road-map
  (`lexique-des-sous-agents`), comme le plan le prévoyait.
- Critère « la commande liste global et local avec leur niveau, sort en erreur sur un terme local
  déjà global ou un doublon » : atteint, vérifié par exécution (variante de casse comprise, sortie
  1, les deux constats nommés).
- Critère « tests passent ; ruff et basedpyright sans régression par rapport à la base » : atteint,
  vérifié. 37 + 120 tests passent ; ensembles d'erreurs identiques à la base pour les deux linters.
- Hors-périmètre : respecté. Diff vide sur `skills/gabarit`, `skills/list-dir`,
  `skills/implementation-tracker` ; aucun niveau de lexique propre aux skills ; aucun terme tiré
  d'une autre skill. La ligne ajoutée à `skills/skill-convention/SKILL.md` est prévue au plan.
- Signaux de dérive : aucun matérialisé. Chaque terme est employé par une skill globale ; chaque
  définition tient en une phrase. `skills/lexique/` ne porte aucun terme (`instructions.md` pose des
  règles, pas des définitions), écart de lecture littérale déjà relevé par `plan-reviewer` et arbitré.
- Élargissement du contrôle 7 : légitime (daté au suivi, accord au journal), testé dans les deux
  sens (ancre sur un fichier existant, ancre sur un fichier absent).
- Symptôme d'origine (« il manque un système de lexique ») : disparu. Un lexique à deux niveaux
  existe, se charge, se contrôle par une commande.
- **R3** — **Écart de vocabulaire relevé nulle part.** Le lexique réserve **Contrat** au sens
  gabarit/list-dir (« Déclaration des champs et des sections qu'un Gabarit ou un Élément doit
  porter »). Or `implementation-tracker` emploie le même mot, avec majuscule, pour un autre concept :
  `references/contrat.md` s'intitule « Contrat du pipeline », sa section « Contrat des sous-agents »
  est citée par `SKILL.md:130` et `references/audit.md:42`. Le lexique lui-même renvoie vers ce
  fichier pour **Étape** et **Base**. Les instructions désormais chargées à chaque session disent
  qu'un terme à majuscule ne s'emploie que dans sa définition : la skill d'où le terme est tiré les
  contredit, soit exactement le cas « gabarit porte deux sens » que le chantier veut prévenir.
  Corriger la prose est hors-périmètre (à raison), mais le plan (étape 4) et le hors-périmètre du
  brief demandaient que les écarts relevés aillent au journal puis au registre de dette ou à la
  road-map. Le journal n'en mentionne aucun et le diff n'ajoute aucune entrée de dette. L'écart
  risque ainsi de rester invisible.

### Qualité du code

- **R4** — `lexique-cli.py`, `_liste` : `lexique liste` prend le répertoire courant comme projet,
  sans remonter à la racine du projet ni lire `$CLAUDE_PROJECT_DIR`, alors que `session` le fait.
  Depuis un sous-répertoire d'un projet muni d'un `.claude/LEXIQUE.md`, la commande répond « local :
  aucun lexique (…/sous/.claude/LEXIQUE.md absent) » et **sort en 0** : constaté par exécution. Le
  contrôle des réservations passe donc en silence, alors que `instructions.md` et `SKILL.md`
  désignent `lexique liste` comme la manière d'obtenir l'état des réservations. `--projet` existe,
  mais rien ne dit à l'agent de le passer.
- **R5** — `lexique.py`, `lire` : deux entrées inattendues sortent du chemin prévu.
  (a) Un fichier non UTF-8 lève `UnicodeDecodeError` jusqu'à la CLI : `lexique liste` imprime une
  trace Python et sort en 1 par hasard, hors du format « un message par ligne » documenté (`session`
  la rattrape correctement). (b) `_blocs` compte toute suite de lignes en `|`, y compris dans un bloc
  de code : un lexique qui montre un exemple de tableau dans sa prose, ce que `SKILL.md` autorise
  (« avec de la prose autour si besoin »), est refusé en « 2 tableaux, un seul attendu ». Constaté
  par exécution. Aucun des deux cas n'est couvert par les tests.
- **R6** — Style, deux restes de retouches. La docstring de `lexique-cli.py` annonce « DEUX RÉGIMES
  D'ÉCHEC » puis en énumère trois (`init` ajouté à l'étape 2 sans mettre le compte à jour).
  `skills/lexique/SKILL.md:53` fait 132 colonnes au milieu d'un paragraphe replié à 100 : l'ajout
  « (terme sans majuscule compris) » n'a pas été replié.
- Pour le reste, rien à signaler : séparation collecte/jugement/CLI conforme à la docstring, fautes
  relevées d'un coup, échec ouvert de `session` effectif (exception rattrapée), racine déduite de
  `__file__` sans constante, garde de version avec `session` silencieuse, nommage et densité de
  commentaires alignés sur `gabarit`. Le `Result` redéfini localement est un choix du plan (stdlib
  seule pour le hook) : il est assumé, ce n'est pas un oubli.

### Dette induite

- **R7** — `settings.json` : le nouveau hook ajoute une seconde constante ancrée sur le `HOME`
  (`python3 $HOME/.claude/skills/lexique/scripts/lexique-cli.py session`). L'entrée de dette
  `hook-sante-chemin-home` (catégorie « pertinent ») repose sur la prémisse que la ligne de
  `sante_skills.py` est « la seule constante ancrée sur le HOME qui subsiste », et prévoit qu'un
  futur contrôle la « tolère nommément ». Cette prémisse est désormais fausse, et l'entrée n'est pas
  mise à jour. Le choix est défendable (un hook ne dépend pas du `PATH`, et le plan le motive), mais
  il reste une exception non écrite. Coût futur : quiconque soldera cette dette sur la foi de
  l'entrée ne traitera qu'une ligne sur deux ; un contrôle qui tolère nommément la première signalera
  la seconde, ou pire, la « corrigera » et le hook cessera de tourner en silence.
- Rien d'autre : le lien `bin/lexique` suit la convention contrôlée par `sante_skills.py`,
  `extraPaths` est tenu à jour, les configurations ruff et pyright de la skill sont identiques à
  celles de `gabarit`.

### Bloquants

Aucun. Les critères vérifiables sont atteints et vérifiés, le hors-périmètre est intact et aucun
signal de dérive ne s'est matérialisé. Le verdict est `RÉSERVES` plutôt que `FAVORABLE` pour trois
raisons : deux critères (R1, R2) ne reposent que sur l'attestation de l'utilisateur ; un écart de
vocabulaire (R3) touche le terme même que le chantier verrouille, sans être consigné ; R4 et R7
laissent chacun un chemin d'échec silencieux. L'utilisateur tranche : clore avec, ou traiter
d'abord.

## 2026-09-23 — clôture — `63b4866`

**Verdict** : DÉFAVORABLE

Périmètre audité : `git diff master...lexique` au commit `63b4866b5f141011a3e074c2c83c26d1c7555249`,
arbre propre, 26 fichiers, +1553 −2. Étapes 1 à 5 cochées `[x]` et commitées. L'étape 5 (R4–R6 de
l'audit `09573af`) est lue en détail par `git diff 09573af 63b4866`. Le brief est présent et lu.
Le suivi reprend ses critères à l'identique, avec un élargissement daté (contrôle 7).

Numérotation : les constats de cet audit reprennent à **R8**, pour que chaque étiquette reste
unique dans ce fichier. Le suivi cite déjà R3 et R7 de l'audit précédent.

### Vérifications exécutées

- `.venv/bin/python -m pytest skills/lexique/scripts/tests -q` → 42 passés, 0 échec (37 + 5
  nouveaux)
- `.venv/bin/python -m pytest scripts/tests -q` → 120 passés, 0 échec
- `lexique liste; echo $?` (racine) → 19 lignes `global`, avis « local : aucun lexique », sortie 0
- `.venv/bin/python scripts/check_pipeline.py` → « Pipeline conforme. », sortie 0
- `python3 scripts/sante_skills.py` → sortie 0
- `grep -n '@' CLAUDE.md` → `1:@RTK.md`, `2:@skills/lexique/instructions.md`, `3:@LEXIQUE.md`
- `git diff --stat master...lexique -- skills/gabarit skills/list-dir skills/implementation-tracker`
  → vide
- `python3 -c "import json; json.load(open('settings.json'))"` → JSON valide
- `uvx ruff check --output-format concise .` sur `git archive master` et sur `git archive 63b4866`
  → 6 / 6 erreurs, ensembles identiques (sorties sans numéros de ligne, diff vide)
- `uvx --with pytest basedpyright` sur les deux mêmes arbres, avec le `.venv` du dépôt lié dans
  chacun → 25 / 25 erreurs, ensembles identiques (diff vide), aucune dans un fichier du chantier
- `(cd skills/lexique && uvx ruff check .)` → « All checks passed! » ; `(cd skills/lexique && uvx
  --with pytest basedpyright)` → 0 erreur
- R4 : `lexique liste` lancée depuis `src/deep` d'un dépôt git muni de `.claude/LEXIQUE.md` →
  terme local listé, sortie 0. **Levée.**
- R5 (a) : lexique local en latin-1 → `lexique liste --projet` affiche « pas en UTF-8, illisible. »
  et sort en 1, sans trace Python. (b) Tableau d'exemple dans un bloc ```` ``` ```` suivi du vrai
  tableau → terme listé, sortie 0. **Levée.**
- R6 : docstring « DEUX RÉGIMES D'ÉCHEC POUR TROIS COMMANDES » ; aucune ligne de prose de
  `skills/lexique/` au-delà de 100 caractères, hors frontmatter. **Levée.**
- Commande exacte du hook (`sh -c 'python3 $HOME/.claude/…/lexique-cli.py session'`) avec
  `CLAUDE_PROJECT_DIR` à la racine d'un dépôt muni d'un lexique, lancée depuis `/` → en-tête et
  tableau, sortie 0
- `lexique` sans argument → usage, sortie 2 ; `lexique bidule` → sortie 2
- **Sous-projet de monodépôt** : dépôt git `$m`, lexique dans `$m/pkg/.claude/LEXIQUE.md`,
  `CLAUDE_PROJECT_DIR=$m/pkg`. `lexique session` au HEAD → **0 octet imprimé**. La même commande
  depuis `git archive 09573af` → « Lexique local de …/pkg » suivi du tableau. Voir R8.
- Depuis `$m/pkg` : `lexique liste` → « local : aucun lexique (`$m/.claude/LEXIQUE.md` absent) »,
  sortie 0 ; `lexique init` → pose `$m/.claude/LEXIQUE.md`, sortie 0. Voir R10.
- HOME versionné simulé (configuration copiée dans `$h/.claude`, `git init $h`, projet hors git
  `$h/projet`) : `lexique liste` → 19 constats « déjà réservé au global », sortie 1. `session`
  imprime le lexique global comme local, avec 19 alertes. Voir R9. `/home/debian` n'est pas un
  dépôt git (`git rev-parse` → « not a git repository »).
- Lexique local en latin-1, `lexique session` → stdout vide, trace sur stderr, sortie 0. Voir R11.
- Clôtures imbriquées (bloc `~~~` qui contient une ligne ```` ``` ````, puis le vrai tableau) →
  « aucun tableau, un seul attendu », sortie 1. Voir R12.
- Critère « une session neuve a le lexique local en contexte » → NON EXÉCUTÉE : aucune session
  Claude Code ne s'ouvre depuis cet audit. Seul le mécanisme est vérifié, au-dessus.

### Conformité à l'intention

- Critère 1 (`grep -n '@' CLAUDE.md`) : atteint, vérifié pour les instructions et le lexique
  global. Le lexique local et l'ordre de chargement sont constatés en deux temps, comme l'utilisateur
  l'a arbitré.
- Critère 2 (`LEXIQUE.md`, tableau à trois colonnes, un lien par ligne) : atteint, vérifié. Le
  fichier n'a pas changé depuis `09573af`.
- Critère 3 (termes proposés puis validés un à un) : inchangé depuis l'audit `09573af`. La
  provenance est vérifiée ; la validation un à un ne repose que sur le journal (R1 de l'audit `09573af`).
- **R8** — Critère 4 (« dans un projet muni d'un `.claude/LEXIQUE.md`, une session neuve a ce
  lexique en contexte ») : **régressé par l'étape 5**. `_session` passe désormais
  `$CLAUDE_PROJECT_DIR` par `racine_projet` :

  ```python
  projet = racine_projet(Path(os.environ.get("CLAUDE_PROJECT_DIR") or Path.cwd()))
  ```

  `$CLAUDE_PROJECT_DIR` désigne déjà le projet que Claude Code a ouvert. Quand ce projet est un
  sous-répertoire d'un dépôt git (un paquet de monodépôt, avec son propre `.claude/`), la commande
  remonte à la racine du dépôt, n'y trouve pas de lexique et **n'imprime rien**. Aucun avis ni
  alerte n'est émis. Constaté par exécution : au HEAD, 0 octet ; à `09573af`, le lexique est
  versé. Ce changement ne répond à aucune demande :
  - R4 ne portait que sur `liste` et notait que `session` lisait déjà `$CLAUDE_PROJECT_DIR` ;
  - le plan (« Chargement du lexique local ») dit que la commande imprime
    `$CLAUDE_PROJECT_DIR/.claude/LEXIQUE.md` ;
  - l'entrée du journal (R4) ne parle que de l'appel sans `--projet`.

  Le `SKILL.md` masque l'écart : « Elle **part** de `$CLAUDE_PROJECT_DIR` ». Aucun test ne couvre
  `session` avec un `CLAUDE_PROJECT_DIR` placé sous la racine du dépôt. Pour un dépôt dont la
  racine est le projet, le critère reste atteint. Pour les autres, il cesse de l'être, en silence,
  par l'effet de la dernière étape.
- Critère 5 (la commande liste global et local, sort en erreur sur une redéfinition ou un
  doublon) : atteint. La logique de `controler` n'a pas changé depuis `09573af`, et les 42 tests,
  qui la couvrent, passent.
- Critère 6 (tests, ruff et basedpyright sans régression) : atteint, vérifié. Les ensembles
  d'erreurs sont identiques à ceux de la base.
- Hors-périmètre : respecté. Le diff est vide sur les trois skills, et aucun niveau de lexique
  propre aux skills n'apparaît.
- Signaux de dérive : aucun matérialisé. `LEXIQUE.md` n'a pas changé ; l'étape 5 ne touche que
  `skills/lexique/`.
- Symptôme d'origine : disparu dans le cas courant, un projet dont la racine est la racine git.
- R1 et R2 de l'audit `09573af` : inchangés. Ils reposent toujours sur l'attestation de
  l'utilisateur au journal.

### Qualité du code

- **R9** — `racine_projet` : sa docstring écarte la remontée jusqu'au premier `.claude/LEXIQUE.md`
  parce qu'« au-dessus d'un projet rangé sous le HOME, la remontée trouverait le lexique GLOBAL et le
  lirait comme local ». La remontée par `git rev-parse` produit exactement ce défaut quand le HOME
  est lui-même un dépôt git (dépôt de dotfiles) et que le projet n'en a pas. Constaté sur une
  simulation : 19 constats « déjà réservé au global », `liste` en 1, et `session` qui verse le
  lexique global comme local, suivi de 19 alertes. Ce n'est pas le cas de cette machine. Pourtant,
  la garantie que la docstring semble donner n'est pas tenue, et rien ne compare la racine trouvée
  à `racine_globale().parent`.
- **R10** — `liste` et `init` sans `--projet` : dans un sous-projet de monodépôt, `init` pose le
  lexique à la racine git, pas dans le `.claude/` du sous-projet. `liste` lit ce lexique-là et
  sort en 0 avec « aucun lexique ». L'utilisateur a choisi la racine git (journal R4). Mais le coût
  de ce choix en monodépôt n'est écrit nulle part, et il sépare la commande de ce que Claude Code
  appelle le projet (`$CLAUDE_PROJECT_DIR`). C'est le même silence que R4, déplacé d'un cas à un
  autre.
- **R11** — `annonce_session` lit le lexique local par `read_text` **avant** `lister`. La correction
  de R5, qui rend `lire` tolérant à un fichier non UTF-8, ne sert donc pas au hook. Sur un lexique
  local en latin-1, l'exception est rattrapée (sortie 0, comme prévu), mais rien n'arrive au
  contexte : ni le lexique, ni l'ALERTE. La trace ne part que sur stderr. La docstring de la CLI
  promet pourtant que les alertes de `session` « vont au contexte ».
- **R12** — `_blocs` bascule sur toute ligne qui commence par ```` ``` ```` ou `~~~`, sans tenir
  compte du type ni de la longueur de la clôture ouvrante. Un bloc `~~~` qui montre une ligne
  ```` ``` ```` décale la bascule, et le vrai tableau est rejeté par « aucun tableau, un seul
  attendu ». L'échec est fermé mais son diagnostic trompe. Ce cas est rare dans un lexique.
- Pour le reste, rien à signaler. Les nouveaux tests sont au style des voisins : un fichier par
  sujet, docstring de module, fixture `ecrire`. `subprocess.run` est borné (`check=False`,
  `OSError` rattrapée), et le passage de `match` à `if` dans `main` reste lisible.

### Dette induite

- **R13** — R3 (« Contrat » employé dans un autre sens par `implementation-tracker`) et R7 (seconde
  constante `$HOME/.claude` dans `settings.json`) sont renvoyés au registre de dette à la clôture
  (journal). Ils n'y sont pas encore : aucune entrée dans `todo/technical-debt/`, et
  `hook-sante-chemin-home.md:13` dit toujours « la seule constante ancrée sur le `HOME` ». Ce
  n'est pas un défaut de l'étape 5, puisque le suivi les place au point 2 de la clôture. C'est un
  rappel : sans ces deux entrées, la clôture effacerait R3 et R7.
- Rien d'autre. `racine_projet` ajoute un appel à `git` au démarrage de chaque session : le coût
  est négligeable, et `OSError` est rattrapée.

### Bloquants

- **R8** : la dernière étape fait régresser le critère 4, en silence, pour un projet dont la racine
  n'est pas celle du dépôt git. Le changement contredit le plan, n'est pas demandé par R4 et n'est
  pas consigné au journal.

Pourquoi `DÉFAVORABLE` plutôt que `RÉSERVES` : dans le cas courant (racine du projet = racine git),
tous les critères vérifiables sont atteints. Mais R8 est une régression d'un critère de réussite,
introduite après l'audit précédent, sans trace, et on ne la voit qu'au résultat. C'est précisément
le cas qu'un audit de clôture doit arrêter. La correction tient en une ligne (`session` reprend
`$CLAUDE_PROJECT_DIR` tel quel, comme avant l'étape 5), avec un test qui la verrouille. R9 à R13
restent des réserves que l'utilisateur tranche.

## 2026-09-23 — clôture — `6b06ca6`

**Verdict** : RÉSERVES

Périmètre audité : `git diff master...lexique` au commit `6b06ca6f4a97abdbdd85adff747305362d36e5fb`,
arbre propre, 26 fichiers, +1766 −2. Étapes 1 à 6 cochées `[x]` et commitées. L'étape 6 (R8
bloquant, R9–R11 de l'audit `63b4866`) est lue en détail par `git diff 63b4866 6b06ca6`. Le brief
est présent et lu. Le suivi reprend ses critères à l'identique, avec un élargissement daté
(contrôle 7) ; l'arbitrage de l'étape 6 est daté au journal, R12 y est renvoyé à la dette.

Numérotation : les nouveaux constats reprennent à **R14**. Les étiquettes antérieures sont citées
pour leur statut.

### Vérifications exécutées

- `.venv/bin/python -m pytest skills/lexique/scripts/tests -q` → 46 passés, 0 échec (42 + 4
  nouveaux)
- Les tests du HEAD rejoués sur le code de `git archive 63b4866` → 4 échecs, exactement les 4
  nouveaux (`test_cli_sous_projet_de_monodepot`, `test_alerte_sur_un_lexique_non_utf8`,
  `test_silencieux_quand_le_local_est_le_global`, `test_lister_ignore_un_local_qui_est_le_global`) :
  ils verrouillent R8, R9 et R11
- `.venv/bin/python -m pytest scripts/tests -q` → 120 passés, 0 échec
- `lexique liste; echo $?` (racine) → 19 lignes `global`, avis « local : aucun lexique », sortie 0
- `.venv/bin/python scripts/check_pipeline.py` → « Pipeline conforme. », sortie 0
- `python3 scripts/sante_skills.py` → sortie 0
- `grep -n '@' CLAUDE.md` → `1:@RTK.md`, `2:@skills/lexique/instructions.md`, `3:@LEXIQUE.md`
- `git diff --stat master...lexique -- skills/gabarit skills/list-dir skills/implementation-tracker`
  → vide
- `find skills -name 'LEXIQUE*' -type f` → aucun fichier
- `python3 -c "import json; json.load(open('settings.json'))"` → JSON valide
- `uvx ruff check --output-format concise .` sur `git archive master` et sur `git archive 6b06ca6`
  → 6 / 6 erreurs, ensembles identiques (sorties sans numéros de ligne ni préfixe d'arbre, diff vide)
- `uvx --with pytest basedpyright` sur les deux mêmes arbres, `.venv` du dépôt lié dans chacun →
  25 / 25 erreurs, ensembles identiques (diff vide), aucune dans un fichier du chantier
- `(cd skills/lexique && uvx ruff check .)` → « All checks passed! » ; `(cd skills/lexique && uvx
  --with pytest basedpyright)` → 0 erreur
- Prose de `skills/lexique/` et `LEXIQUE.md`, hors frontmatter et lignes de tableau : aucune ligne
  au-delà de 100 caractères
- **R8** — dépôt git `$m`, lexique dans `$m/pkg/.claude/LEXIQUE.md`, commande exacte du hook
  (`sh -c 'python3 $HOME/.claude/skills/lexique/scripts/lexique-cli.py session'`) lancée depuis `/`
  avec `CLAUDE_PROJECT_DIR=$m/pkg` → en-tête « Lexique local de …/pkg » puis le tableau, sortie 0.
  Même commande avec `CLAUDE_PROJECT_DIR` à la racine d'un dépôt muni d'un lexique → versé,
  sortie 0. **Levé.**
- **R9** — HOME simulé versionné par git (configuration du HEAD extraite dans `$h/.claude`,
  `git init $h`) :
  - `lexique liste` depuis `$h/projet` sans lexique → 19 termes `global`, 0 constat, avis « local :
    …/h/.claude/LEXIQUE.md est le lexique global, ignoré comme local. », sortie 0 ;
  - `session` avec `CLAUDE_PROJECT_DIR=$h` → 0 octet, sortie 0 ;
  - `session` avec `CLAUDE_PROJECT_DIR=$h/projet` → lexique du projet versé ;
  - `~/.claude` en lien symbolique vers la configuration, `CLAUDE_PROJECT_DIR=$HOME` → 0 octet.

  Le symptôme relevé est levé. Un reste subsiste, voir R14.
- **R10** — depuis `$m/pkg` : `lexique liste` → « local : aucun lexique (`$m/.claude/LEXIQUE.md`
  absent) », sortie 0 ; `lexique liste --projet .` → `local Zorglubage`, sortie 0. Le comportement
  est inchangé, mais il est désormais écrit dans `SKILL.md` avec la parade `--projet`. **Levé par
  la documentation**, comme l'utilisateur l'a arbitré.
- **R11** — lexique local en latin-1, hook exact → en-tête, « (illisible — voir l'alerte
  ci-dessous) », ALERTE « pas en UTF-8, illisible. », sortie 0. Lexique local en `chmod 000` → même
  forme, ALERTE « illisible (Permission denied) », sortie 0 ; `lexique liste --projet` sur ce fichier
  → sortie 1. **Levé.**
- Critère 5 : un lexique local portant `SUIVI`, `Essai` et `ESSAI` → `lexique liste --projet` sort
  en **1** et nomme les deux constats (« « ESSAI » défini deux fois (lignes 4 et 5) » ; « « SUIVI »
  (ligne 3) est déjà réservé au global (« Suivi », ligne 18) »). `session` sur le même fichier →
  les deux sous « ALERTE », sortie 0.
- `lexique liste` depuis un sous-répertoire d'un dépôt muni d'un lexique (R4) → terme local listé,
  sortie 0
- `lexique` sans argument → sortie 2 ; `lexique bidule` → sortie 2
- Critère « une session neuve a le lexique local en contexte » → NON EXÉCUTÉE : aucune session
  Claude Code ne s'ouvre depuis cet audit sans écrire hors du fichier d'audit. Seul le mécanisme est
  vérifié, au-dessus.

### Conformité à l'intention

- Critère 1 (`grep -n '@' CLAUDE.md`) : atteint, vérifié pour les instructions et le lexique
  global, dans cet ordre. Le lexique local et l'ordre global → local sont constatés en deux temps,
  comme arbitré : le hook est vérifié ici, la session neuve est attestée au journal de l'étape 3.
- Critère 2 (`LEXIQUE.md`, tableau à trois colonnes, un lien par ligne) : atteint, vérifié. 19
  lignes, chacune avec un lien. `check_pipeline.py` confirme les cibles. Le fichier n'a pas changé
  depuis `09573af`.
- Critère 3 (termes proposés puis validés un à un) : **invérifiable** sur pièces, inchangé (R1).
  La provenance des 19 termes est vérifiée. La validation un à un ne repose que sur le journal.
- Critère 4 (lexique local en contexte dans une session neuve) : le mécanisme est rétabli et
  vérifié, y compris pour le sous-projet de monodépôt que R8 avait cassé. `session` lit de nouveau
  `$CLAUDE_PROJECT_DIR` tel quel, comme le plan le décrit et comme à l'étape 3. La session neuve
  elle-même reste attestée par l'utilisateur seul (R2), et cette attestation date de l'étape 3.
  Depuis, `session` a changé deux fois et revient au même comportement, à deux ajouts près : la
  garde « local = global » et la lecture rattrapée.
- Critère 5 (la commande liste global et local, sort en erreur sur une redéfinition ou un
  doublon) : atteint, vérifié par exécution, variante de casse comprise.
- Critère 6 (tests, ruff et basedpyright sans régression) : atteint, vérifié. 46 + 120 tests
  passent, et les ensembles d'erreurs des deux linters sont identiques à ceux de la base.
- Hors-périmètre : respecté. Le diff est vide sur les trois skills. Aucun niveau de lexique propre
  aux skills n'apparaît, et aucun terme n'est tiré d'une autre skill (Brief et Hors-périmètre sont
  liés à `intent-brief` mais employés par `implementation-tracker`, comme relevé en `09573af`).
- Signaux de dérive : aucun matérialisé. `LEXIQUE.md` n'a pas bougé, chaque définition tient en une
  phrase, aucun fichier `LEXIQUE*` n'existe sous `skills/`, et l'étape 6 ne touche que
  `skills/lexique/`.
- Symptôme d'origine (« il manque un système de lexique ») : disparu. Un lexique à deux niveaux
  existe, se charge et se contrôle, y compris pour un sous-projet de monodépôt.

### Qualité du code

- **R14** — `racine_projet` et `_est_le_global` : **reste de R9** quand le HOME est versionné par
  git. La garde neutralise le faux conflit, mais `liste` et `init` sans `--projet` visent toujours
  `$HOME`, dont le `.claude/LEXIQUE.md` est le global. Constaté sur la simulation :
  - un projet `$h/projet` muni de son propre `.claude/LEXIQUE.md` (terme `Zorglubage`) :
    `lexique liste` lancée depuis ce projet **n'en liste aucun terme** et sort en 0. Le seul avis
    est « …/h/.claude/LEXIQUE.md est le lexique global, ignoré comme local » ; il ne dit pas que le
    lexique du projet a été manqué ;
  - `lexique init` depuis `$h/autre` → « …/h/.claude/LEXIQUE.md existe déjà : rien n'est écrasé »,
    sortie 1. L'échec est fermé et ne détruit rien, mais le diagnostic désigne le global comme si
    c'était le local du projet.

  La docstring de `racine_projet` écarte toujours la remontée jusqu'au premier
  `.claude/LEXIQUE.md` parce qu'elle « trouverait le lexique GLOBAL ». La remontée par git le trouve
  aussi dans ce cas. `SKILL.md` ne signale la parade `--projet` que pour le monodépôt. `session`,
  qui lit `$CLAUDE_PROJECT_DIR` tel quel, n'est pas touchée : le lexique du projet arrive bien au
  contexte. Ce n'est pas le cas de cette machine, où `/home/debian` n'est pas un dépôt git.
  L'utilisateur a demandé que R9 soit traité : il est levé pour le symptôme qui avait été relevé
  (le global lu comme local, 19 faux constats), pas pour le cas voisin.
- Le reste de l'étape 6 est propre :
  - la garde compare des chemins résolus, ce qui tient avec un `~/.claude` en lien symbolique
    (vérifié) ;
  - le texte de repli de `annonce_session` renvoie à une alerte que `lister` produit effectivement,
    pour un fichier non UTF-8 comme pour un fichier illisible (vérifié) ;
  - le commentaire de `_session` porte sa règle en capitales, comme le veut la convention ;
  - les quatre tests suivent le style des voisins (fixture `ecrire`, helper `_session`) et
    échouent sur le code précédent ;
  - l'annotation `lo: Result[list[Terme]]` passe basedpyright en mode `all`.
- R12 (clôtures de code imbriquées) : comportement inchangé. L'utilisateur l'a renvoyé au registre
  de dette.

### Dette induite

- R13 reste ouvert, et le suivi le reporte au point 2 de la clôture : R3, R7 et désormais R12 n'ont
  pas encore d'entrée dans `todo/technical-debt/`. `hook-sante-chemin-home.md:13` dit toujours
  « C'est la seule constante ancrée sur le `HOME` ». Sans ces trois entrées, la clôture les
  effacerait.
- Rien d'autre à signaler. L'étape 6 n'ajoute ni duplication ni couplage : `_est_le_global` sert
  aux deux appelants, et `session` perd l'appel à `git` que l'étape 5 lui avait ajouté.

### Bloquants

Aucun. R8 est levé et verrouillé par un test qui échoue sur le code précédent. Tous les critères
vérifiables sont atteints et vérifiés, le hors-périmètre est intact, et aucun signal de dérive ne
s'est matérialisé.

Le verdict est `RÉSERVES` plutôt que `FAVORABLE` pour trois raisons :
- deux critères (3 et 4) reposent en partie sur la seule attestation de l'utilisateur (R1, R2) ;
- R14 laisse un chemin où `liste` sort en 0 sans avoir lu le lexique du projet, dans un cas rare ;
- les trois entrées de dette (R3, R7, R12) restent à écrire avant d'aplatir.

L'utilisateur tranche : traiter R14, le documenter, ou le porter en dette avec les autres.
