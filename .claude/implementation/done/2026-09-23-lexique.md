+++
gabarit = "suivi"
slug = "lexique"
titre = "Un lexique global et local qui cadre et définit le sens des mots"
branche = "lexique"
base = "master"
statut = "terminé"
session = 1
lettre = "A"
execution = "direct"
plan = ".claude/implementation/done/2026-09-23-lexique.plan.md"
brief = ".claude/implementation/done/2026-09-23-lexique.brief.md"
audit = ".claude/implementation/done/2026-09-23-lexique.audit.md"
"créé" = 2026-09-23
maj = 2026-09-23
+++

## Objectif et périmètre

**Symptôme** : « il manque un système de lexique pour cadrer et définir le sens des mots ».

**But** : chaque terme défini a un seul sens, que ni l'agent ni l'utilisateur n'emploient pour
autre chose ; les termes se définissent à deux niveaux — `LEXIQUE.md` global, `.claude/LEXIQUE.md`
local à un projet — chargés automatiquement, et un terme réservé au global ne peut plus être
défini en local.

**Critères de réussite** :
- `grep -n '@' CLAUDE.md` → le fichier d'instructions du lexique est importé, et le chargement
  des lexiques global puis local vient après lui
- `LEXIQUE.md` existe à la racine de `~/.claude` : un tableau `terme | définition courte | lien`,
  chaque ligne avec un lien vers un élément pertinent
- Les termes du lexique global sont tirés de `gabarit`, `list-dir` et `implementation-tracker`,
  proposés puis validés un à un par l'utilisateur
- Dans un projet muni d'un `.claude/LEXIQUE.md`, une session neuve a ce lexique en contexte, sans
  action de l'utilisateur
- La commande de lexique liste les termes global et local avec leur niveau, et sort en erreur quand
  un terme local est déjà défini au global ou qu'un terme apparaît deux fois
- Les tests de la commande passent ; `uvx ruff check` et `uvx --with pytest basedpyright` sans
  régression par rapport à la base

**Hors-périmètre** :
- Aligner la prose de `gabarit`, `list-dir` et `implementation-tracker` sur les termes retenus :
  chantier à part ; les écarts relevés vont au registre de dette ou à la road-map
- Un niveau de lexique propre aux skills
- Des termes tirés d'autres skills que `gabarit`, `list-dir` et `implementation-tracker`

**Signaux de dérive** :
- Le lexique global accueille un terme qu'aucune skill globale n'emploie, ou une définition qui
  dépasse une phrase
- Le diff touche la prose de `gabarit`, `list-dir` ou `implementation-tracker`
- Un lexique apparaît dans `skills/<nom>/`, ou une skill charge son propre lexique

**Élargissement du 2026-09-23** : le contrôle 7 de `scripts/check_pipeline.py` ignore l'ancre d'un
chemin de skill cité — `LEXIQUE.md` porte des liens ancrés, que le contrôle prenait pour des
fichiers inexistants.

## Étapes

- [x] 1. Bibliothèque et commande `lexique` — skills/lexique/scripts/, ruff.toml, pyrightconfig.json, pyrightconfig.json racine, bin/lexique — vérif: `.venv/bin/python -m pytest skills/lexique/scripts/tests -q` + linters racine comparés à `git archive master`
- [x] 2. Instructions, skill, lexique global vide, imports de `CLAUDE.md`, `lexique init` — skills/lexique/{instructions,SKILL}.md, skills/lexique/scripts/, LEXIQUE.md, CLAUDE.md, skills/skill-convention/SKILL.md — vérif: `grep -n '^@' CLAUDE.md && lexique liste && .venv/bin/python scripts/check_pipeline.py`
- [x] 3. Hook `SessionStart` du lexique local — settings.json — vérif: `CLAUDE_PROJECT_DIR=$d python3 skills/lexique/scripts/lexique-cli.py session` + session neuve par l'utilisateur
- [x] 4. Amorcer le lexique global — LEXIQUE.md, scripts/check_pipeline.py, scripts/tests/test_controle_7.py — vérif: `lexique liste` → 0 ; `git diff --stat master -- skills/gabarit skills/list-dir skills/implementation-tracker` vide
- [x] 5. Traiter les réserves R4–R6 de l'audit de clôture — skills/lexique/ — vérif: `.venv/bin/python -m pytest skills/lexique/scripts/tests -q` + nouvel audit complet
- [x] 6. Traiter R8 (bloquant) et les réserves R9–R11 du second audit — skills/lexique/ — vérif: `.venv/bin/python -m pytest skills/lexique/scripts/tests -q` + nouvel audit complet

## État courant

**Prochaine action** : aplatir et archiver (`git-smart-commit`, type 3).

**Vérification** : `.venv/bin/python -m pytest skills/lexique/scripts/tests -q && lexique liste && .venv/bin/python scripts/check_pipeline.py`

**Dernier audit** : `6b06ca6` — RÉSERVES — 2026-09-23

**Notes** : critère 1 constaté en deux temps (arbitrage du plan) — le grep pour instructions et global, la session neuve de l'étape 3 pour le local et l'ordre. Linters racine : se comparent sur des copies des fichiers suivis (`git archive master` contre `git ls-files -co --exclude-standard`) — l'arbre réel porte des répertoires ignorés qui font monter basedpyright à ~5 500 erreurs. Référence : ruff 6, basedpyright 25.

## Journal de décisions

- **2026-09-23** — Commande, instructions et procédure dans une skill-outil `skills/lexique/`, sans aucun terme. *Pourquoi* : une commande globale est déclarée par une skill (commandes-locales.md). *Rejeté* : fichiers à la racine et `scripts/lexique.py`.
- **2026-09-23** — Lexique local chargé par un hook `SessionStart` global qui lance `lexique session`, en échec ouvert. *Pourquoi* : `CLAUDE.md` global n'importe que depuis son répertoire. *Rejeté* : un import à ajouter dans le `CLAUDE.md` de chaque projet.
- **2026-09-23** — Verdict `plan-reviewer` NON CONFORME ; l'utilisateur retient les deux corrections (extraPaths racine + linters comparés à la base ; critère 1 en deux temps). *Pourquoi* : écarts de mesure, pas d'intention. *Rejeté* : élargir le brief.
- **2026-09-23** — `lexique liste` dit un lexique vide ou absent, par niveau, sur stderr et en code 0 (demande de l'utilisateur). *Pourquoi* : un état légitime, pas un constat. *Rejeté* : le faire échouer.
- **2026-09-23** — `lexique init` pose le lexique local, en-tête compris, et refuse d'écraser (relecture `skill-convention`, accord de l'utilisateur). *Pourquoi* : les scripts font la structure, le modèle n'écrit que des lignes. *Rejeté* : en-tête recopié à la main ; semence `gabarit`, qui imposerait un front matter.
- **2026-09-23** — Session neuve vérifiée par l'utilisateur : instructions, puis global, puis local (sortie du hook) ; le terme local est connu. Un sous-agent `Explore` ne reçoit **rien** — ni `CLAUDE.md`, ni ses imports, ni le hook. *Pourquoi* : limite de Claude Code, prévue au plan. *Suite* : entrée de road-map `lexique-des-sous-agents`, au choix de l'utilisateur. *Rejeté* : dette ; élargir le périmètre.
- **2026-09-23** — Tout terme porte une majuscule initiale dans le lexique et dans son emploi défini ; la minuscule garde le sens courant ; `lexique liste` refuse un terme sans majuscule ; la réservation reste insensible à la casse (demande de l'utilisateur). *Pourquoi* : réserver des mots courants sans interdire leur sens ordinaire. *Rejeté* : majuscule pour les seuls mots communs, qui exigerait de juger ce qui est commun.
- **2026-09-23** — Lexique global amorcé : 19 termes validés par l'utilisateur, dont les mots courants rendus réservables par la majuscule (Plan, Base, Clôture…). « Répertoire-liste » renommé **List-dir** ; Semence couvre gabarit et list-dir ; Étape se réfère au Suivi, qui peut s'écarter du Plan. *Pourquoi* : choix de l'utilisateur. *Rejeté* : Définition de liste, Tag d'étape, Aplatissement (hors des trois skills ou trop proches de la colonne « Définition »).
- **2026-09-23** — Élargissement : le contrôle 7 du garde-fou ignore l'ancre d'un chemin cité (accord de l'utilisateur). *Pourquoi* : 6 faux « chemin inexistant » sur `LEXIQUE.md`. *Rejeté* : retirer les ancres ; laisser en dette avec un garde-fou rouge.
- **2026-09-23** — Trois audits de clôture : `09573af` RÉSERVES (R4–R6 traités, étape 5), `63b4866` DÉFAVORABLE (R8 : l'étape 5 avait aussi fait remonter `session` à la racine git ; R8–R11 traités, étape 6), `6b06ca6` RÉSERVES. R3, R7, R12 et R14 versés au registre de dette. *Pourquoi* : arbitrages de l'utilisateur. *Rejeté* : clore avec toutes les réserves dès le premier audit.
- **2026-09-23** — Sans `--projet`, `init` et `liste` prennent la racine git du répertoire courant ; `session` reprend `$CLAUDE_PROJECT_DIR` tel quel ; un local qui est le fichier global est ignoré. *Pourquoi* : lancée d'un sous-répertoire, la commande concluait à l'absence du lexique, et le sous-projet ouvert d'un monodépôt porte le sien. *Rejeté* : remonter jusqu'au premier `.claude/LEXIQUE.md`, qui trouverait le global au-dessus d'un projet rangé sous le HOME.
