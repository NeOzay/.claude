# Revue du registre — 2026-08-17

Registre : `.claude/implementation/todo/technical-debt.md`
**Entrées au registre : 14 — blocs instruits : 14.**

`git status --short` à la fin de la passe, avant toute correction de chantier :
`?? .claude/implementation/done/revues/` — cette seule ligne. Aucun fichier de code touché.
Recopié ici parce que le rapport archivé est la **seule pièce qui survit à la passe** : l'arbre de
travail, lui, est propre dès le commit suivant, et l'observation ne serait plus reproductible.

Trois entrées d'abord classées `aggravee` ont été **reclassées `pertinent` après correction** :
l'aggravation venait du chantier `revue-dette` lui-même et a été annulée avant arbitrage. Le
détail de chaque annulation, avec la commande qui l'établit, est dans le bloc concerné.

Les six entrées où le chantier `revue-dette` avait lui-même modifié le terrain décrit ont été
instruites par une contre-expertise indépendante, en contexte isolé, avant d'être recoupées ici.

**Arbitrage du 2026-08-17.** Trois questions étaient ouvertes ; deux ont été tranchées.

1. **Les numéros de ligne sont proscrits du registre** — trop fragiles. Une entrée désigne désormais
   un fichier, une section, une phrase citée. La règle est écrite dans `dette.md`, « Gabarit
   d'entrée », et les **19** repères de ligne que portaient les entrées ont été remplacés par des
   citations : un `grep` des motifs `fichier:NNN` entre accents graves en compte **19 sur `master`**
   et **0** après. Cela règle du même coup les trois pointeurs périmés relevés par cette revue.
2. **Une entrée vraie mais mal écrite se corrige sur place** — nouvelle section « Corriger une
   entrée » dans `dette.md` : ni solde, ni mise à l'écart, ni aggravation, et donc pas de huitième
   catégorie. Date et intitulé ne bougent pas ; la correction porte sa commande, comme un solde.
3. **La catégorie « sentinelle » reste ouverte** — les deux entrées dont le *Pour solder* dit
   « rien » restent `pertinent`, sans marqueur. À rejuger si la revue suivante les voit revenir.

# Aggravée

## aggravee | 2026-08-14 | Le chemin de la skill est codé en dur et répété en trois points d'édition

**Vérifié par** — `git grep -c '\$HOME/\.claude/skills' master -- skills` → 3 occurrences
(`implementation-tracker/SKILL.md` ×2, `contrat.md` ×1). `git grep -c … HEAD -- skills` → **5** :
les deux mêmes fichiers, plus `debt-review/SKILL.md:129` et `:143`.

**Verdict** — toujours vrai, et **3 → 5 points d'édition**. Le **Pour solder** proposait une
résolution du chemin de skill au lieu d'une constante ; ce chantier a reconduit la constante deux
fois pour un second script. Aucun des cinq n'échoue bruyamment s'il est oublié.

**Action** — laisser au registre, réécrire le **Constat** avec le chiffre de 5. **À noter** : le
contrôle 6 du garde-fou **impose** la forme `$HOME/…` — cette aggravation était structurellement
forcée, et ne se solde pas sans traiter le problème générique.

**Arbitré** — appliqué. Constat réécrit et daté, marqueur `(aggravée) 2026-08-17` posé. Le **Pour
solder** est complété : seule la résolution du chemin empêche le compte de croître, le contrôle 6
imposant la forme à chaque skill neuve.

# Pertinent

## pertinent | 2026-08-14 | Trois lignes du corpus dépassent l'enroulement à 100 colonnes

**Vérifié par** — comptage en **caractères** sur tout le corpus versionné (`git ls-tree -r
--name-only <ref> -- skills scripts hooks`, filtré `.md`/`.sh`, `python3` pour la longueur) :
`master` → **133** lignes au-delà de 100 ; `HEAD` → **134**. Sur le seul
`skills/implementation-tracker/` : **28 sur `master`, 28 sur `HEAD`**. Les trois lignes nommées par
l'entrée sont intactes au caractère près (`contrat.md:7` = 121, `:15` = 131, `dette.md:122` = 140).

**Verdict** — le **Constat est faux d'un facteur 44** : il annonce trois lignes « que le reste du
corpus respecte », alors que 133 dépassaient déjà avant ce chantier. L'entrée n'a pas été aggravée,
elle a été **sous-mesurée dès son écriture**. Apport propre du chantier : **une** ligne,
`skills/debt-review/scripts/trier-revue.sh:35` (110 caractères, tableau `LIBELLES`) — et zéro dans
le corpus du tracker malgré 70 lignes ajoutées à `contrat.md` et `dette.md`.

**Reclassée après correction** — la ligne `trier-revue.sh:35` a été ré-enroulée : l'arbre est
retombé à **133**, le compte exact de `master`. Le chantier n'a plus aucun effet sur cette entrée,
donc `aggravee` ne se soutient plus.

**Action** — laisser au registre. **À arbitrer, au titre du contenu faux** : le Constat annonce
trois lignes « que le reste du corpus respecte » alors que 133 dépassaient déjà. Aucune des sept
catégories ne nomme un constat **sous-mesuré dès son écriture** — `pertinent` le laisse tel quel,
`aggravee` dirait faussement qu'il s'est étendu.

**Arbitré** — corrigé sur place au titre de « Corriger une entrée ». Le Constat porte les 133 lignes
réelles et sa commande de mesure ; l'intitulé, faux, est conservé comme clé de référence et le
Constat le dit. Le **Pour solder** demande désormais la seconde décision — convention outillée ou
état accepté — sans laquelle l'entrée reviendra à chaque revue.

## pertinent | 2026-08-14 | La table d'empreintes du garde-fou est maintenue à la main

**Vérifié par** — `sed -n '/^fingerprints=(/,/^)/p' scripts/check-pipeline.sh | grep -c "^  '"` →
**8 empreintes**, identique à `master`, pour **7** sections de contrat. Et
`grep -rn '(catégorie) date' --include='*.md' skills` → **2 fichiers** :
`implementation-tracker/references/dette.md:182` et `debt-review/references/categories.md:20`.

**Verdict** — toujours vrai, et le risque que l'entrée décrivait comme hypothétique s'est
matérialisé : ce chantier a écrit quatre règles neuves (deux sections de `dette.md`, deux règles de
`contrat.md`) sans une seule empreinte, et **la règle du marqueur est désormais écrite deux fois** —
exactement ce que le contrôle 2 existe pour interdire, et il est aveugle faute d'empreinte.

**Reclassée après correction** — la duplication a été supprimée : `categories.md` renvoie désormais
à
`dette.md` pour la forme du marqueur et n'en garde que la liste des catégories concernées.
`grep -rl '(catégorie) date' --include='*.md' skills` → **1 fichier**. Le dommage concret ayant
disparu, l'entrée retombe à son état d'origine.

**Action** — laisser au registre. **À arbitrer, au titre du contenu faux** : le **Pour solder** est
inexécutable — il propose « autant d'empreintes que de sections `##` », or il y en a 8 pour 7
sections sur un dépôt sain. Le contrôle échouerait à son premier lancement, sur `master` comme ici.

**Arbitré** — corrigé sur place. Le **Pour solder** demande maintenant un contrôle qui rattache
**chaque empreinte à la section qu'elle protège** et échoue sur une section sans empreinte. La
formulation d'origine aurait fait retirer une empreinte légitime pour faire passer le contrôle.

## pertinent | 2026-08-14 | Deux renvois du dépôt ne se suivent pas depuis l'éditeur

**Vérifié par** — résolution des chemins par `os.path.normpath` depuis le répertoire de chaque
fichier : `categories.md:70`, `categories.md:86` et `gabarit-rapport.md:87` écrivent
`` (`dette.md`, « … ») `` → cible `skills/debt-review/references/dette.md` : **NON RÉSOLU** dans les
trois cas. `dette.md` vit dans `implementation-tracker/references/`. Les deux renvois nommés par
l'entrée sont intacts (`impl-list.sh:6`, `implementation-auditor.md:95`).

**Verdict** — toujours vrai. Les deux renvois nommés par l'entrée sont intacts et toujours non
suivables.

**Reclassée après correction** — le chantier avait porté le compte de 2 à 5 en reproduisant le
défaut d'`impl-list.sh:6` dans trois renvois de forme `` (`dette.md`, « … ») ``. Les trois chemins
ont été corrigés en `../../implementation-tracker/references/dette.md` : contrôle de résolution sur
tout `skills/debt-review/` → **0 renvoi non résolu**. Retour à 2.

**Action** — laisser au registre, inchangée.

## pertinent | 2026-08-14 | Les patterns de sécurité de `git-pre-commit-audit` ne sont plus branchés

**Vérifié par** — `ls archive/git-pre-commit-audit/references/` → 9 fichiers, les huit grilles par
langage et `security-patterns.md`, tous présents. Et
`grep -rl 'git-pre-commit-audit' --include='*.md' . | grep -v '^./archive/'` → uniquement des
archives de chantiers clos dans `done/`, aucun point de chargement actif.

**Verdict** — inchangé : la capacité existe, intacte, et rien ne l'appelle.

**Action** — laisser au registre.

## pertinent | 2026-08-14 | Une étape corrective née d'un audit ne peut pas être cochée avant l'audit

**Vérifié par** — `grep -n 'toutes les étapes' skills/implementation-tracker/references/cloture.md`
→ ligne **23** : « Vérifier ensuite que toutes les étapes sont cochées ». L'exigence est intacte.

**Verdict** — inchangé sur le fond. La référence de l'entrée est en revanche périmée : elle cite
`cloture.md:10`, la règle est aujourd'hui en `:23`. Le constat tient, le pointeur non.

**Action** — laisser au registre, et **arbitrer** ce qu'on fait d'un numéro de ligne périmé : rien
dans le dispositif ne le dit aujourd'hui.

**Arbitré** — les numéros de ligne sont proscrits du registre. L'entrée cite désormais la phrase
« toutes les étapes sont cochées » de `cloture.md`, qui ne se périme pas au déplacement.

## pertinent | 2026-08-14 | `impl-list.sh` dépend d'une extension GNU de `find`

**Vérifié par** — `grep -n 'printf' skills/implementation-tracker/scripts/impl-list.sh` → ligne
**31** : `-printf '%f\n' | sort`. Le numéro de ligne de l'entrée est exact.

**Verdict** — inchangé, au caractère près.

**Action** — laisser au registre.

## pertinent | 2026-08-14 | Un critère de réussite du brief `contrat-pipeline` était faux dans sa lettre

**Vérifié par** — `grep -l 'contrat' agents/*.md` → **3 fichiers**
(`implementation-auditor.md`, `step-implementer.md`, `plan-reviewer.md`), là où l'entrée en décrit
2. Le critère « aucun résultat » reste inatteignable, et l'est davantage.

**Verdict** — le fait tient, et le chiffre a bougé : un troisième agent est né depuis. Ce n'est pas
une aggravation du **problème** — celui-ci est la formulation figée d'un brief clos — mais du
constat qui le décrit.

**Action** — laisser au registre. **À arbitrer** : son **Pour solder** dit « rien sur ce
chantier-ci ». L'entrée est une leçon figée, pas une dette actionnable ; elle repassera `pertinent`
à chaque revue.

## pertinent | 2026-08-14 | `git-smart-commit` n'est plus lisible sans `implementation-tracker`

**Vérifié par** — `grep -n 'implementation-tracker' skills/git-smart-commit/references/squash.md`
→ ligne 8, `[Branche et
commits](../../implementation-tracker/references/contrat.md#branche-et-commits)`. La dépendance est
en place, telle que décrite.

**Verdict** — inchangé.

**Action** — laisser au registre. **À arbitrer**, pour la même raison que l'entrée précédente : son
**Pour solder** est « rien tant que le tracker ne bouge pas ».

## pertinent | 2026-08-14 | Le garde-fou de pipeline a cinq angles morts connus

**Vérifié par** — `git diff --stat master...HEAD -- scripts/check-pipeline.sh` → vide, script
intact. Les cinq angles morts sont en place : portée `skills` seule (`:47`, `:66`, `:76`, `:202`),
`slugify` ne retirant que l'apostrophe (`:37`), contrôle 2 en `grep -rl … | wc -l` donc comptant des
fichiers (`:98`), auto-citation à `contrat.md:15`, garde textuel non positionnel (`:199`).

**Verdict** — inchangé. Les repères de l'entrée (`:34 :44 :63 :95 :197`) ont dérivé de +2/+3, sans
qu'aucun des cinq défauts soit touché.

**Action** — laisser au registre. **À signaler** : le champ **Assumé** de l'entrée (« aucune de ces
situations n'existe dans le dépôt aujourd'hui ») est faux, et l'était déjà avant ce chantier — des
ancres mortes `contrat.md#autorité` et `contrat.md#ancre` existent hors de `skills/`, dans les
archives de `done/`. Elles sont en prose d'exemple, ce qui atténue sans annuler.

**Arbitré** — l'**Assumé** est corrigé sur place : il porte « aucune de ces situations n'existe
**dans `skills/`** », et une ligne `Corrigé le` établit le renvoi mort par commande. Les cinq
repères de ligne des angles morts sont remplacés par les extraits de code qu'ils désignaient.

## pertinent | 2026-08-14 | La clause « arbre propre » du tracker ne couvre pas le plan

**Vérifié par** — `git diff --stat master...HEAD -- skills/implementation-tracker/SKILL.md` → vide.
`sed -n '97,103p'` de ce fichier : la clause ne connaît qu'une exception, `*.brief.md` ; aucune
mention de `.claude/plans/`. Et `git check-ignore -v .claude/plans/witty-tickling-metcalfe.md` → non
ignoré, donc le plan remonte bien en `??` au moment du contrôle.

**Verdict** — inchangé, mot pour mot. L'arbitrage hors procédure a été rejoué une seconde fois à
l'ouverture de ce chantier.

**Action** — laisser au registre. `aggravee` écartée sciemment : l'entrée **prédit** que « chaque
ouverture de chantier rejouera l'arbitrage ». Une récidive annoncée par le constat le confirme, elle
ne l'étend pas.

## pertinent | 2026-08-16 | Le point 3 du tracker repose sur une prémisse de harness sans repli écrit

**Vérifié par** — `sed -n '124,126p' skills/implementation-tracker/SKILL.md` → « Le harness assigne
un fichier de plan dès l'entrée en plan mode et en autorise l'écriture », suivi d'aucune phrase de
repli. Aucune occurrence de repli dans le point 3.

**Verdict** — inchangé : la prémisse est toujours nue.

**Action** — laisser au registre.

# Invérifiable en revue

## inverifiable | 2026-08-14 | Les correctifs de l'étape 9 de `dette-technique` n'ont jamais été audités

**Vérifié par** — `grep -n '^## ' .claude/implementation/done/2026-08-14-dette-technique.audit.md`
→ trois sections, la dernière `## 2026-08-14 — clôture (ré-audit complet) — 343f180`. Aucune section
postérieure. C'est le seul fait établissable ; qu'un audit *n'ait pas eu lieu* ne se lit nulle part.

**Verdict** — fait historique. Le premier volet du **Pour solder** (« auditer le diff `master…
dette-technique` de l'étape 9 ») reste actionnable, mais le constat lui-même ne peut ni être
confirmé ni infirmé par une lecture du dépôt.

**Action** — laisser au registre, marquer `(invérifiable en revue) 2026-08-17` sous le titre.

## inverifiable | 2026-08-14 | Le correctif `R21` de `contrat-pipeline` n'a jamais été audité

**Vérifié par** — `grep -n '^## ' .claude/implementation/done/2026-08-15-contrat-pipeline.audit.md`
→ trois sections, la dernière `## 2026-08-14 — clôture — 0c5a8fe`, précisément le commit que
l'entrée nomme. Rien après.

**Verdict** — fait historique, même patron que l'entrée sur l'étape 9 de `dette-technique`.

**Action** — laisser au registre, marquer `(invérifiable en revue) 2026-08-17` sous le titre.

